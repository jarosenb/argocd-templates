apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../env-config-testing-base/

namePrefix: envconftest-

configMapGenerator:
  - name: env-cm
    envs:
      - conf.env
  