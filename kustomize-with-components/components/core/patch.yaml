apiVersion: apps/v1
kind: Deployment
metadata:
  name: componentized-nginx 
spec:
  template:
    spec:
      containers:
        - name: core-nginx
          image: nginx:stable-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              
